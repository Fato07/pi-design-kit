---
description: Review UI/design work for hierarchy, fidelity, accessibility, and polish
argument-hint: "[system] <file-or-brief>"
---
Use the `design-review` skill from `pi-design-kit`.

User args: `$ARGUMENTS`

Review the referenced artifact, route, file, or brief. If the first argument names a bundled design system, compare against that system. Otherwise use the project `DESIGN.md` or existing project conventions. Provide prioritized findings and a patch plan; only edit files if explicitly requested.
